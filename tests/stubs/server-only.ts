/* Stub de `server-only` para vitest.
 *
 * El paquete real lanza al resolverse con la condicion de navegador, que es la
 * que usa vitest por defecto. En Next si protege de verdad: el build falla si
 * un componente cliente importa un modulo que lo incluya.
 */
export {}
